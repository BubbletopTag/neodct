fun icons for the engineering apps, no more hard hats

Replaced the placeholder icons with real 120x120 transparent PNGs
(sources from "neodct icons/", baked-in transparency checkerboards
removed):

- CubeBench: white 3D cube cut off its checker background (local
  flatness + convex hull, since the cube faces match the checker colors)
- KeypadMapper: access-keypad photo, checker background removed
- KeypadMapperI2C: same keypad with a blue I2C corner badge (both
  source jpgs were byte-identical)
- Crash: red-ball/white-X redrawn crisp at 4x and downscaled (source
  png was a tiny icon nearest-neighbor upscaled, hopelessly pixelated)
- LCDTest: monitor icon trimmed, padded square and rescaled
- Tests: new clipboard icon, two green checks and one red X

FuelGauge (diesel can) and LinuxShell (Tux) keep their icons.
Also noted in the 0.2.4a CHANGELOG Engineering section.

---

kernel: persist SIM7600 modem drivers in qemu config, add option + qmi_wwan

The /dev/ttyUSB* mystery solved: menuconfig had enabled the USB-serial
core (CONFIG_USB_SERIAL) but never the actual modem driver
(CONFIG_USB_SERIAL_OPTION), so nothing bound to the SIM7600's serial
interfaces even though lsusb saw 1e0e:9001. Those USB changes also lived
only in the ephemeral output/build .config — a clean rebuild would have
silently lost them.

board/qemu/aarch64-virt/linux.config:
- wrote back the existing menuconfig work via `make linux-update-defconfig`
  (USB_ACM, USB_SERIAL, USB_SERIAL_GENERIC, USB_USBNET, USB_NET_RNDIS_HOST)
- added CONFIG_USB_SERIAL_OPTION=y: the Qualcomm/SIMCom serial driver;
  1e0e:9001 is in its device table (SIMCOM_PRODUCT_SIM7100E, shared with
  SIM7600), so it auto-binds and creates ttyUSB0-5 — no new_id hack needed
- added CONFIG_USB_NET_QMI_WWAN=y: gives wwan0 at the default PID 9001,
  dialable with plain AT$QCRMCALL=1,1 over the AT port — kernel driver
  only, no libqmi/ModemManager userspace needed

Also .claude/settings.json: allow background Claude sessions to edit
this checkout directly (worktrees can't reach the buildroot output tree).

---

MODEM SUPPORT: real ModemService, boot auto-connect, live signal bars

Three-stage bring-up for the SIM7600G on T-Mobile, all BusyBox + kernel
drivers + Python -- no ModemManager/libqmi/pyserial (64 MB RAM budget):

- engineering/tools/atcmd: scriptable one-shot AT commands from the
  shell, lock-safe against the UI and boot script (terminal PoC stage).
- /etc/init.d/S45modem: backgrounded boot bring-up -- SIM check, IPv6
  PDP context (fast.t-mobile.com), CEREG registration wait, AT$QCRMCALL
  data call, wwan0 up + SLAAC + DNS64 nameservers. Config via
  /etc/default/modem, log at /tmp/modem-boot.log.
- ModemService rewritten from stub to real AT driver: raw termios port
  ownership, sysfs interface-number port probe with AT->OK confirm,
  staggered CSQ/CEREG/COPS polling pumped from read_keypress (same
  chokepoint as the battery tick), URC state machine (RING/+CLIP/VOICE
  CALL/NO CARRIER) with a latched event queue, real ATD;/AT+CHUP dial
  and hangup, Simulation Mode fallback with /tmp sim hooks, hotplug
  re-probe every 10 s.
- Home screen sig icon_set is live: 0-4 bars from real tower CSQ, 0 when
  unregistered, layout sim_val in Simulation Mode.
- Home screen "No Service" text is live too: shows the registered
  operator's short name (AT+COPS=3,1 -> "Tello"/"T-Mobile") and falls
  back to No Service when unregistered/simulated.
- Real call placement gated behind system.modem.allow_calls (default
  OFF): dial/answer pretend (2 s fake connect) until the voice path is
  ready, so Call can't ring a real number by accident. AT+CHUP still
  goes out on hardware (idle-safe, rejects real incoming rings).
- docs/MODEM_BRINGUP.md runbook + 0.3.0a changelog section.

- New Modem engineering app (id 9005): three softkey-walked status pages
  (RADIO / SIM / DATA) in the FuelGauge style -- operator/CEREG/CSQ+dBm,
  CPIN/phone number/IMEI/ICCID/IMSI/firmware (one-shot queries), and
  S45modem status/wwan/IPv6/APN/DNS. Stays useful in Simulation Mode
  (shows which ttyUSB nodes exist). Placeholder signal-bars icon pending
  real art.

Sim-mode state machine covered by an ad-hoc test sweep (bars mapping,
dial/connect/hangup, ring hook, URC parsers, snapshot), and the Modem
app's row builders by a stub-modem sweep (incl. CICCID->CCID fallback).

---

MusicPlayer: album art fix — sidecar folder-art fallback

Album art never showed for the on-device library. Root cause: those
mp3s carry full ID3v2.4 text tags but no embedded APIC frame at all
(verified with mutagen + raw scan for JPEG/PNG magic), and the player
only ever looked for APIC.

- get_metadata now falls back to sidecar art next to the track:
  cover/folder/front/album/albumart + .jpg/.jpeg/.png, case-
  insensitive. Works for .wav/.aac and without mutagen too.
- APIC loop now tries every APIC frame instead of giving up after
  the first unreadable one.

Verified offscreen with a FakeUI harness driving the real
run_now_playing: sidecar cover.jpg renders, embedded-APIC file still
renders, no-art file still gets the note placeholder. Drop a
cover.jpg into /NeoDCT/User/music/ (or the album folder) for art on
the current library.

---

MusicPlayer: swap mpv subprocess for in-process miniaudio streaming

Playing a track no longer spawns mpv (~24MB private RSS, the OOM
killer's first pick on the 64MB targets). The new _MiniaudioPlayer
streams the file chunk-by-chunk through python-miniaudio into one
ALSA device -- kilobytes of buffer, no spawn latency, and a single
ALSA client (same reasoning as Koki's mixer).

- pause/resume = device stop/start with the same stream generator
  (it keeps its position), replacing SIGSTOP/SIGCONT on mpv
- progress bar shows the real decoded-frame position; the wall-clock
  estimate remains only for the mpv fallback
- end-of-track via stream_with_callbacks end_callback
- stream decode buffer sized to the device period (default 500ms,
  NEODCT_MUSIC_ABUF_MS overrides): miniaudio raises mid-callback if
  the device asks for more frames than frames_to_read allocated --
  found by the headless test; the track would hang silent forever
- mpv kept as automatic fallback when miniaudio is missing or its
  device fails to init; a bad file (DecodeError) does NOT demote the
  session to mpv. NEODCT_MUSIC_AUDIO=subprocess|miniaudio forces a
  backend (same convention as NEODCT_KOKI_AUDIO)
- flac/ogg now scanned + playable; aac only via the mpv fallback
  (miniaudio has no aac decoder)
- wav/flac/ogg durations via miniaudio.get_file_info (mutagen only
  handled mp3)
- BR2_PACKAGE_PYTHON_MINIAUDIO=y added to qemu + luckfox defconfigs
  (it lived only in the live .config; a clean rebuild would drop it)

Verified headlessly (NULL backend, real app module, UI stubbed):
play/pause/resume/position, end detection, replay after stop,
bad-file and device-failure fallback paths, plus a real mp3 decode
sweep -- 17 checks + mp3, all green. Also noted in the 0.3.0a
CHANGELOG MusicPlayer section.

---

Messages: real SMS sending -- FIRST LIVE TELEPHONY

Write Message -> Send now prompts for a number and hands the draft to
the SIM7600 instead of the "requires Telephony... by M3" placeholder
(also replaced on the Outbox draft Send option).

- ModemService.send_sms(number, text): text-mode AT+CMGS with bespoke
  ">"-prompt handling (the prompt has no newline, so the line-based
  reader can't see it), 30 s network-ack window, ESC bailout when the
  prompt never comes, +CMS/+CME error passthrough, ascii-safe body.
  Sim Mode pretends and says so. No allow_calls-style gate: SMS is
  the intended first live telephony feature.
- New ContactNumberInput in Messages: TextInput subclass where any
  arrow key opens the shared PhoneBook contact selector (first real
  use of that groundwork) and autofills the contact's number. Its
  (row, index) return is unpacked correctly -- note the Dialer's
  existing target[2] unpack of the same selector is off-by-a-layer
  and still needs the same fix.
- App-side guardrails: empty and >160-char drafts stopped with a
  dialog before touching the modem; "Sending..." screen during the
  network wait; success/failure dialogs with the +CMGS ref or reason.
- Receiving deliberately not wired yet (one step at a time).

send_sms verified end-to-end against a pty fake modem speaking the
real CMGS dance (prompt, body+Ctrl-Z, +CMGS ref, CMS ERROR reject),
plus validation and sim paths. Messages app py_compile clean.

---

SMS receiving + NotifyService: BEEP BEEP, "1 message received"

The other half of telephony round one. New NotifyService
(System/core/NotifyService) does Nokia-3310 home-screen notifications
(match the reference photo): tone immediately, "N message(s) received"
banner mid-left with the carrier line hidden, softkey -> Read, C
dismisses, flashing envelope in the status strip while unread mail
exists. Deliberately NOT a modal framework -- nothing pops over running
apps, and battery warnings keep their existing separate path.

- ModemService: AT+CNMI=2,1 push notifications (+CMTI URC -> event, no
  busy-polling), fetch_sms() (CMGR + CMGD -- messages are deleted from
  the SIM so its ~30 slots never fill and silently stop reception),
  read_stored_sms() boot sweep (CMGL "REC UNREAD") for messages that
  arrived while the phone was off, busy/ok/error statuses with polite
  event requeue when S45modem holds the port.
- main.py glue in _modem_tick: modem events -> sms_inbox.db (existing
  schema, zero Messages-app changes needed to list them) ->
  notify.post_sms. Read softkey loads the Messages app module and jumps
  straight into the message (single) or the Inbox (several).
- Messages app: open_message()/open_inbox() entry points; opening a
  message now marks it read (is_read existed but nothing ever set it).
- Tone: user's "nokia sms" rip converted to mono 22 kHz wav (silence
  trimmed, loudness normalized) at System/tones/sms.wav; played via
  fire-and-forget aplay -- skips silently if the ALSA device is busy.
  Invisible to the Tones ringtone list (it only scans mp3/wma).
- New envelope.png status icon (stdlib-rasterized like the ModemInfo
  placeholder), blinking at 2 Hz next to the signal bars.
- Sim hook: echo '5551234|hey' > /tmp/neodct_sim_sms fakes an arrival
  in QEMU without passthrough.

Verified against the pty fake modem: boot sweep with multi-line bodies,
live CMTI->CMGR->CMGD, requeue ordering, sim hook, banner
pluralization/dismiss. All four touched files py_compile clean.

---

REAL VOICE CALLS: USB audio via CPCMREG + aplay, dial beeps

Receive-audio-only call support (no mic hardware yet; test target is
MCI's 1-800-444-4444 readback line):

- allow_calls default flipped to ON -- the audio path exists now.
- dial(): ATD, then IMMEDIATELY AT+CPCMREG=1, then pipe the PCM port
  (USB interface 4, auto-detected via sysfs like the AT port) to the
  speaker: `aplay -q -t raw -f S16_LE -r 16000 -c 1 /dev/ttyUSBn` as a
  fire-and-forget subprocess. Early media/ringback plays if the network
  sends it. answer() does the same for the future incoming-call UI.
- Teardown everywhere kills aplay and defers AT+CPCMREG=0 to the next
  free poll tick (URC handlers already hold the port lock): End key,
  NO CARRIER, VOICE CALL: END, MISSED_CALL, service close().
- call_screen: "Calling..." label until VOICE CALL: BEGIN, and the
  screen exits on its own when the far end hangs up (modem.state IDLE
  via the read_keypress URC pump) instead of waiting for End.
- Fake DTMF beeps while typing a number on the home screen: 12 proper
  dual-tone 16 kHz wavs (0-9/star/hash, 120 ms, 5 ms anti-click ramps)
  in System/tones/dtmf/, played via NotifyService.play_tone. VoLTE
  never hears them; they're pure Nokia vibes.

pty fake-modem tests: ATD->CPCMREG=1 ordering, remote-end teardown with
deferred CPCMREG=0, hangup path, allow_calls=OFF pretend fallback, DTMF
wav format sweep. py_compile clean on ModemService/main/call_screen.

---

call monitoring: CLCC progress, audio-path logging, stuck-call recovery

First live test: calls connect ("Call 1" = VOICE CALL: BEGIN arrived)
but there was zero visibility into the audio path. Now:

- dial() logs CPCMFRM/CPCMREG results; CPCMREG=1 re-asserted on the
  first tick after connect (some firmwares reject it pre-call).
- AT+CPCMFRM explicitly set: factory default is 8 kHz on many builds
  while we play at 16 kHz -- the prime silent/garbled-audio suspect.
  system.hw.modem_pcm_rate=8000 flips modem + aplay together.
- AT+CLCC polled every 2 s in-call: "Call progress: CALLING/RINGING/
  CONNECTED" in the console, "Ringing..." label + mm:ss timer on the
  call screen, and empty-CLCC recovery that ends a stuck call state
  when VOICE CALL: END was missed.
- aplay watchdog: exit code logged, pipe restarted (3 s holdoff).
- All call URCs now printed to the console.

pty fake-modem tests: ATD->CPCMFRM->CPCMREG ordering, CLCC state
progression driving call_status(), empty-CLCC stuck recovery with
"ended" event, in-call CPCMREG retry.

---

INCOMING CALLS: the phone rings, interrupts apps, Answer/decline

Round one of telephony is complete -- NeoDCT now receives calls.

- IncomingCall(BaseException) raised from read_keypress (the chokepoint
  every screen funnels through) unwinds whatever app is running. It
  derives from BaseException exactly like KeyboardInterrupt so an app's
  `except Exception` can't swallow a ringing phone; unwinding still runs
  the apps' finally blocks, so MusicPlayer/Koki release the ALSA device
  before the ringtone starts. launch_app/render_menu re-raise it instead
  of showing the crash screen; the core loop catches it and rings. The
  3310 didn't multitask either -- suspend/resume is post-alpha.
- New System/ui/Dialer/incoming_screen.py, built to the 3310 photo:
  caller name or number across the top (auto-shrunk; a full USA number
  never runs off), flashing "calling" bottom-left, softkey "Answer",
  C declines. Phonebook lookup matches on the last 10 digits so a stored
  "555-1234" resolves a "+1616555..." caller ID. A late +CLIP updates
  the label live; _ring_tick waits one poll cycle for it so the screen
  doesn't open on "Unknown" (withheld numbers still ring after 0.6 s).
  Caller hangs up first -> screen returns "gone" on its own.
- NotifyService gained the ringer: system.audio.ringtone decoded once
  and looped in-process via python-miniaudio (no ~24MB mpv), mpv as
  fallback. Paths with spaces never touch a shell. Resolution chain
  never rings silently: exact path -> same stem with another extension
  (survives the .wma->.mp3 re-encode) -> Nokia Tune/Ring Ring -> any
  playable tone, each step logged.
  NOTE: settings.prop says "Brave Scottland.mp3"; the file is "Brave
  Scotland.mp3" (one t) -- rings the fallback until that's fixed.
- SettingsStorage default ringtone .wma -> .mp3.
- Sim hook fix: /tmp/neodct_sim_ring rings once per write/touch
  (mtime-tracked). It used to re-ring instantly after a decline, making
  the decline path untestable; `rm` while ringing still = caller gave up.
- Ring volume control in Tones is deliberately left for later.

Tested against the real main.py/NotifyService/ModemService code (PIL and
UI stubbed): exception semantics through a greedy app handler + finally,
_ring_tick CLIP grace and withheld-number path, answer/decline/gone
flows asserting ring-stop happens before answer takes ALSA, ringtone
resolution (typo, space, wma->mp3, unset), loop generator wrap, and the
sim ring hook lifecycle.

---

incoming screen: shift "calling" clear of the signal column

First real incoming call screenshot showed "calling" overlapping the
bottom of the signal-bar icons. The label now starts just right of the
column, derived from the same scaling render_element uses for status
icons (36px asset at x=7 scaled by H/240 -> ends at x=33; text at x=39).

---

default ringtone is Low.mp3 across the board

- SettingsStorage DEFAULTS, the shipped User/settings.prop, and the head
  of NotifyService's never-silent fallback chain all agree on
  /NeoDCT/System/tones/Low.mp3 now (was a mix of Entertainer and the
  Brave Scotland test value).

---

FULL-DUPLEX CALLS: mic uplink via arecord into the PCM port

The other half of the audio path -- a complete NeoDCT OS phone call.
The SIM7600's CPCMREG stream is bidirectional, so the uplink mirrors
the downlink with pure alsa-utils plumbing:

    speaker <- aplay   <- /dev/ttyUSBn   (far end's voice, existing)
    far end <- /dev/ttyUSBn <- arecord   (USB sound card mic, NEW)

- arecord -q -t raw -f S16_LE -r <pcm_rate> -c 1 -D <dev> <port>,
  spawned next to aplay at dial/answer, killed with it at teardown.
  arecord ships in the image already (alsa-utils APLAY installs both).
- system.hw.modem_mic_device picks the capture device ("default";
  "plughw:N,0" to pin; OFF/NONE for deliberate listen-only).
- Watchdogs split: speaker restarts indefinitely (3 s holdoff), the mic
  gets three attempts then gives up into listen-only with a logged hint
  (`arecord -l`) -- a guest without a capture device is a normal state,
  not something to spin on.
- Real hardware uses an embedded stripped USB sound card = the exact
  same snd-usb-audio path as QEMU passthrough; this plumbing is final.
  If QEMU's usb-audio turns out playback-only (`arecord -l` empty),
  virtio-sound-pci does capture and CONFIG_SND_VIRTIO=y already ships.

Tested with recorded-Popen fakes over a pty PCM port: dual spawn with
correct args, mic three-strikes -> listen-only (then stays quiet),
speaker restart, hangup kills all live pipes, OFF/custom device
settings pass through. py_compile clean.

---

mic uplink: auto-detect the capture card (QEMU usb-audio is playback-only)

First full-duplex field test confirmed QEMU's usb-audio device exposes
zero capture endpoints (arecord -l empty in the guest) -- the mic pipe
did its 3-strikes give-up as designed. The fix is passing the USB mic
itself through (same snd-usb-audio path as the real phone's embedded
sound card), which creates a second guest sound card... where ALSA's
"default" (card 0 = playback-only QEMU USB Audio) becomes a trap.

- system.hw.modem_mic_device now defaults to AUTO: scan /proc/asound
  for the first cardN with a pcm*c (capture) node -> plughw:N,M.
  Explicit devices still pass through; OFF still means listen-only.
- No capture device found = one clean log line and a listen-only call,
  not three arecord deaths.
- Also confirmed in the field: CPCMREG=1 -> ERROR at dial time on this
  firmware, then OK on the post-connect retry -- the retry path earns
  its keep.

Tested against a fake /proc/asound tree: playback-only card 0 + mic on
card 1 -> plughw:1,0; capture on card 0 (real-hardware shape) ->
plughw:0,0; no capture -> mic pipe skipped; hotplug found on next call.

---

i2c: all bus defaults 1 -> 3 (real hw scan) + version bump to 0.3.0a

i2cdetect on the board found the whole peripheral chain on i2c-3
(PCF8575 keypad @ 0x20, MAX1704x gauge @ 0x36); every driver assumed
bus 1, so on hardware BatteryService would have probed /dev/i2c-1 and
silently run Simulation Mode (fixed 3.85 V).

- system.hw.battery_i2c_bus: SettingsStorage default "1" -> "3", plus
  explicit override in User/settings.prop
- BatteryService DEFAULT_I2C_BUS, pcf8575_keypad DEFAULT_BUS (feeds
  KeypadMapperI2C and the keymap.json it writes), core/main.py
  keymap i2c_bus fallback, max1704x_watch --bus default: all 1 -> 3
- NOTE: keymap.json files generated before this carry i2c_bus 1;
  re-run KeypadMapperI2C on units mapped earlier

Version 0.3.0a everywhere: SettingsStorage defaults (was 0.2.4a),
launcher.py splash (0.2.4a), etc/os-release (0.2.2a!), and the
BR2_TARGET_GENERIC_ISSUE banner in qemu/luckfox/radxa defconfigs
(0.2.3a). settings.prop + docs/CHANGELOG.txt were already 0.3.0a.
Re-run make <defconfig> before the next luckfox build or the stale
issue banner in build-luckfox/.config ships again.

py_compile clean on all six touched .py files; historical 0.2.4a
references in BatteryService's docstring left alone on purpose.

---

Add T9 multi-tap typing: UI framework + LinuxShell console driver

Shared engine (System/hw/t9_engine.py, pure logic, no I/O): NeoDCT
keycodes in, edit ops out (append/replace/mode). Modes abc/ABC/123
cycled with #; 2-9 multi-tap letters, 1 punctuation, 0 space, digit at
end of each cycle. Input filters: any / letters / numbers (numbers =
literal digits + * # for phone fields, no mode switching).

UI side (framework.py): TextInput and TextInputLong take
input_filter=, expose handle_key(), and draw an abc/ABC/123 indicator.
T9 runs only when ui.matrix_input (i2c keypad) exists; QEMU/dev
QWERTY path unchanged but now respects the filter. PhoneBook name
fields are letters-only, number fields numbers-only;  Messages
ContactNumberInput is numbers-only and now types * / # on the keypad.

LinuxShell side (System/hw/t9_uinput.py): virtual keyboard via
/dev/uinput (pure stdlib struct/ioctl, 32-bit-safe input_event
packing) + T9ShellBridge thread that polls ui.matrix_input and
injects console key events; multi-tap cycling sends backspace+char,
enter/clear/arrows pass through (NeoDCT codes ARE Linux keycodes).
Created when LinuxShell opens on keypad hardware, destroyed on exit
(UI_DEV_DESTROY) -- never alive while the UI owns the keypad, absent
on QEMU where a real keyboard exists.

Kernel: CONFIG_INPUT_MISC + CONFIG_INPUT_UINPUT added to the shared
board/qemu/aarch64-virt/linux.config (all three flavors use it;
without uinput the shell still works, bridge just stays off).

Tests: neodct/tests/ host-side pytest suite (68 tests, TDD:
engine, widget handle_key paths, uinput event bytes via pipe fd,
bridge lifecycle, LinuxShell wiring). Not shipped: overlay prune
only covers overlay/tests, and neodct/tests sits outside the overlay.

--- netsurf-neodct: NeoDCT browser port (branches: netsurf@neodct, libnsfb@neodct) ---
netsurf: port old WebKit browser UI onto netsurf 3.11 framebuffer frontend

- Pin netsurf checkout to release/3.11 (matches host system component libs)
- New frontends/framebuffer/neodct/: TDD'd chrome logic (128 checks green:
  neodct_url, neodct_menu, neodct_cursor, neodct_ui state machine,
  neodct_status) + neodct_shell.c fbtk chrome (URL bar, status bar,
  softkey bar, Options menu, Input Text popup, keypad cursor)
- gui.c: NeoDCT window creation, key routing through neodct shell,
  240x175 default, NULL-furniture guards, hover/status/url hooks
- Dev harness: NEODCT_SCRIPT env drives scripted input + PPM screenshots
  headlessly on the ram surface (unit tests: make -C frontends/framebuffer/neodct/test)

libnsfb: restore Linux framebuffer surface, drop windowed deps

- Restore src/surface/linux.c (removed upstream in b2bb565, pre-0.2.2);
  compiles clean against current surface API, gated by NSFB_LINUX_AVAILABLE
- Build with only linux+ram surfaces (no SDL/X/VNC/wayland)

--- netsurf-neodct: QEMU/buildroot integration ---
libnsfb@neodct: add evdev keyboard input to linux surface

- New src/surface/linux_evdev.c: evdev keycode -> nsfb keycode table
  (TDD: test/evdev.c wired into libnsfb test harness)
- linux.c: open /dev/input/event* keyboards at init (NSFB_INPUT_DEV
  env overrides), poll()-based input() honouring timeout, autorepeat
  as key-down; no more busy-looping stub

buildroot: netsurf 3.11 + NeoDCT sources, enable for QEMU aarch64

- package/netsurf: bump to netsurf-all-3.11, POST_EXTRACT hook rsyncs
  local netsurf-neodct/{netsurf,libnsfb} neodct branches over bundled
  trees; framebuffer build uses linux surface, internal font, no JS;
  drop obsolete patches (0001/0003/0004) and stale libnsfb pkg dep;
  framebuffer is now a proper frontend choice in Config.in
- configs/neodct_qemu_defconfig: resync from live .config (was stale:
  v0.2.1a vs live v0.2.3a) + add libcurl, netsurf, netsurf-framebuffer
- overlay: new apps/Browser (id 11) launching /usr/bin/netsurf-fb with
  LinuxShell-style takeover + t9_uinput bridge on keypad hardware;
  home.html ported from the old WebKit browser homepage
- verified in headless QEMU (monitor sendkey/screendump): chrome, menu,
  typing, cursor + real evdev input on fbdev; https fetch works (curl
  200 on example.com)

--- netsurf-neodct: fix BadEncoding on target ---
buildroot/netsurf: build libparserutils without the iconv filter

Pages with no declared charset (HTML5 Windows-1252 fallback) failed with
about:query/fetcherror "BadEncoding": libparserutils' default iconv input
filter needs glibc gconv modules that the purged target rootfs doesn't
ship (uclibc has no usable iconv either). Define WITHOUT_ICONV_FILTER via
libparserutils/Makefile.config.override so the builtin charset codecs are
used. Verified in QEMU: https://example.com typed into the URL bar over
virtio-keyboard renders correctly on the framebuffer.

--- netsurf-neodct: NeoDCT-styled chrome, font, mem log, exit fix ---
netsurf@real-browser-tests: framework-faithful chrome + Nokia font

- Overlay screens redrawn to replicate framework.py exactly, in one
  imperatively-drawn full-screen widget: VerticalList Options menu
  (font_xl title, 11-N breadcrumb, white selection box, gray scrollbar
  with notch), TextInput "Go to URL" (prompt, outlined box, blinking
  cursor, tail-scroll clip), TextInputLong "Input Text" (char count,
  font_s greedy word wrap - TDD'd neodct_wrap), SoftKeyBar (30px,
  centred font_n, only over menus/inputs)
- freetype fontlib with the NeoDCT font for all faces (option defaults
  point at /NeoDCT/System/ui/resources/fonts/font.ttf; NEODCT_FONT env
  overrides for host dev); page content renders in the phone font too
- neodct-mem: rss/hwm from /proc/self/status (TDD'd parser) printed to
  stderr every 5s -> serial console (~13.4MB RSS on target with
  example.com loaded)

buildroot: netsurf fb build switches to freetype fontlib (+freetype dep)

overlay Browser app: drain queued keypresses on exit (TDD'd, 4 pytest
cases) - keypad fd backlog + i2c scanner pending queue made the UI
replay stale presses as random menu actions after the browser closed;
browser stderr now goes to /dev/console for the mem log

--- netsurf-neodct: zram, scoped fonts, crash + system memory logging ---
system: zram swap + crash visibility for the browser

- kernel (qemu board config): CONFIG_ZRAM/ZSMALLOC/LZO backends
- overlay etc/init.d/S16zram: compressed swap sized to half of RAM
  (busybox swapon -p fallback), swappiness 100
- netsurf chrome: fatal-signal handler prints "neodct-crash: signal=N
  last-rss-kb=X" async-safely on stderr/serial; periodic mem log now
  includes whole-system pressure (MemAvailable/MemFree/SwapFree)
- Browser app: reports how netsurf-fb exited ("KILLED by signal 11
  (SIGSEGV)" etc, TDD'd) and dumps a dmesg tail on signal deaths so
  OOM-killer reports surface even with a quiet console

fonts: Nokia face only for chrome and home.html, DejaVu for the web

- the system font now rides the fantasy/cursive faces (chrome widgets,
  NeoDCT screens and home.html's font-family: fantasy); sans/serif/
  monospace default to DejaVu (buildroot dejavu package added to the
  qemu defconfig) so real sites like wikipedia render normally

--- modem: self-healing data bring-up -- the modem is now the network device ---

S45modem v2 (overlay etc/init.d/S45modem):
- APN walk: wholesale (Tello, our SIM -- user confirmed) first, then
  fast.t-mobile.com and tello.com; a rejected $QCRMCALL or missing
  address moves to the next candidate instead of giving up
- MODEM_RAW_IP=auto is now really automatic: call up but no SLAAC in
  20s -> flip qmi_wwan to raw-ip framing and re-dial (many SIM7600
  firmwares only speak raw-ip)
- carrier DNS from AT+CGCONTRDP (AT+CGPIAF colon notation) goes into
  resolv.conf ahead of the Google DNS64 fallback
- winning APN + framing cached in /etc/modem.cache -> next boot
  connects first-try; delete the file to re-probe
- wwan0 prepped (accept_ra, framing, link up) BEFORE dialing so the
  RS/RA exchange happens the moment the bearer rises
- stop now also downs the wwan interface

target tooling for an IPv6-only carrier:
- busybox PING6 via new board/qemu/busybox.fragment (wired into both
  neodct_qemu_defconfig copies): ping -6 / ping6 exist on target
- BR2_PACKAGE_LIBCURL_CURL=y: real curl binary on target for
  DNS64/NAT64/TLS debugging (same libcurl netsurf uses)

proof harness:
- scripts/qemu_modem_data_test.py: boots the image headless with the
  modem passed through (-snapshot; rootfs untouched), logs in over the
  serial socket, waits for S45modem's verdict, then checks global IPv6,
  default route, ping6, DNS resolution, HTTP and HTTPS fetch -- one
  command proves "internet purely from the modem"
- docs/MODEM_BRINGUP.md: Stage 2 rewritten for the self-healing flow,
  new Stage 4 (end-to-end proof), APN/ping6 troubleshooting rows

- fix from first on-air run: eudev predictable naming calls the qmi_wwan
  interface wwp0s5u3i5, not wwan0 -- S45modem and the Modem eng app now
  find it by bound driver (qmi_wwan) with name prefixes as fallback

- dialer reworked after 2nd on-air run (instant NO CARRIER x3 APNs while
  registered on Tello, CSQ 16): APN was right (host NetworkManager
  profile "Tello" = wholesale, verified via nmcli) so the suspects are
  PDP type and the rewrite-cid1-while-attached quirk. Now: PDP-type walk
  (IPV6 then IPV4V6 -- the proven mmcli bearer was ipv4v6), modem's own
  cid1 definition tried early, PS re-attach (CGATT 0/1) between dial
  rounds, one quick retry per dial, 4 rounds then endless background
  redial every 120s (weak basement RF "eventually works" -- calls do),
  AT+CEER reject-cause + AT+CPSI? RSRP logging on every rejection,
  log rotation for the forever-retry path

- 3rd on-air run cracked it open: +CGACT showed cid 2 ALREADY ACTIVE
  (the attach/IMS PDN -- calls worked mid-failure) while every dial of
  cid 1 got "+CEER: ESM sync up with network" ~1s after registration.
  Dialer now: waits MODEM_ESM_SETTLE_S (8s) after CEREG before dialing,
  logs the FULL context table, and each round first tries riding an
  already-active non-IMS context (AT$QCRMCALL=1,<cid> untouched) before
  walking APNs on cid 1; QCRMCALL/CGCONTRDP/teardown are cid-aware
  (/tmp/modem-cid), rounds gate on current registration (basement drops
  service mid-walk), ridden bearers are never cached as APNs

- 4th run: cid 1 (fast.t-mobile.com IPV4V6) is ACTIVE per +CGACT -- the
  network granted the internet PDN at attach -- yet $QCRMCALL still
  instant-fails, now with "+CEER: EMM detached" (contradicted by
  CEREG=1 + active bearers = stale internal state; ModemManager's QMI
  session survives USB passthrough into QEMU). Countermeasures:
  one-time AT+CFUN=1,1 module reset after a failed dial cycle
  (MODEM_RESET_ON_FAIL=y), AT$QCRMCALL? state in diagnostics, and a
  real QMI fallback -- BR2_PACKAGE_UQMI in both defconfigs, S45modem
  ends each round with uqmi --start-network on /dev/cdc-wdm0 (the exact
  path the host's proven ModemManager connection used); wwan prep
  extracted to prep_wwan() and re-run after reset; non-numeric cid
  (qmi) handled in DNS readback + teardown

- buildroot pkg fixes for host cmake >= 4 (Arch): libubox.mk + uqmi.mk
  get -DCMAKE_POLICY_VERSION_MINIMUM=3.5 (upstream ships pre-3.5
  cmake_minimum_required and configure hard-fails otherwise)

--- luckfox: kernel + rootfs caught up to the qemu feature set ---

SDK kernel (luckfox-pico repo, luckfox_rv1106_linux_defconfig):
- modem-as-network-device: USB_NET_DRIVERS/USB_USBNET/USB_NET_QMI_WWAN,
  USB_WDM, USB_SERIAL_OPTION (AT ports on real hw)
- browser support: ZRAM/ZSMALLOC/CRYPTO_LZO (S16zram swap),
  INPUT_MISC/INPUT_UINPUT (t9_uinput bridge)
- all verified present in the built objs_kernel/.config; boot.img
  rebuilt via distrobox ./build.sh kernel
- build gotcha fixed: objs_kernel/scripts held x86 host tools built on
  native Arch (glibc 2.38+) -- modpost exploded inside the Ubuntu 22.04
  distrobox; rm -rf objs_kernel/scripts and rebuild in the box

neodct rootfs (luckfox_pico_mini_defconfig, both copies synced):
- netsurf framebuffer + libcurl(+curl bin) + uqmi + busybox PING6
  fragment + alsa aplay (call downlink pipe was missing on hw!) +
  BR2_TOOLCHAIN_BUILDROOT_CXX (live config had it, canonical lacked it)
- build-luckfox live .config regenerated from the defconfig

--- docs: HTML documentation site + headless UI stub ---

New host-side tooling (neodct/tools/, never shipped to the device):

- uistub.py: headless stub of the whole runtime. Boots the REAL
  NeoDCT_UI/framework/apps with no framebuffer, keypad, modem or
  /NeoDCT rootfs, and captures frames as PIL images.
  * CapturingFramebuffer stands in for core.main.Framebuffer and can
    composite the 240x175 band onto the 240x240 panel (device_frame)
  * PathRemap redirects hardcoded /NeoDCT/... paths into a staged copy
    of the overlay -- no root, no chroot, and writes never touch the
    working tree (found a real gap: settings.prop is saved via
    os.rename, which needs BOTH path args remapped)
  * KeyScript replays evdev codes; when it drains it raises
    ScriptExhausted (a BaseException, like IncomingCall) so blocking
    widget loops unwind instead of hanging
  * frame budget stops real-time apps that never poll read_keypress at
    all (Koki reads ui.keypad_fd directly) at the one choke point every
    app passes through: fb.update()
  * install_app() copies an app folder in and rescans, i.e. exactly
    what "installing" means on the device
  * simulate_status() fakes a fuel gauge + registered modem so doc
    shots show a normal device instead of "?" / "No Service"
  * never attaches to a real input device -- the runtime's evdev
    fallback to /dev/input/event0 is stubbed out too, so the harness
    cannot race the developer's own keyboard

- shoot_docs.py: renders all 55 documentation screenshots (home, app
  selector, every stock + engineering app, call/ring/crash screens,
  the widget gallery, the tutorial examples) in under a minute.

- tests/test_uistub.py: 37 tests, written test-first. Cover the device
  geometry (240x175 band centred on the 240x240 panel, band starts at
  row 32), path remapping and restoration, key scripting, app scanning,
  wallpaper loading, and both loop-escape mechanisms. Skipped when PIL
  is unavailable; the pre-existing 76 tests are untouched (113 total).

Documentation site lives OUTSIDE the repo at ../neodct-docs (8 pages):
index, how-it-works, stock-apps, apps (tutorial + full ui reference),
widgets, modem, hardware, building. White-on-black with the shipped
Nokia Cellphone FC face, hand-authored SVG diagrams, no JS deps beyond
a ~70-line syntax highlighter. Modem and system pages are written for
readers who have never seen an AT command. Three tutorial apps
(HelloNeoDCT, Dice, Countdown) ship with the docs with 8 tests of their
own.

Note: the README status table is now well out of date -- it still lists
telephony/SMS/settings as unimplemented. The docs page reflects 0.3.0a.

- ROOT CAUSE FOUND (in-lab QEMU loop, stable cable): the days of instant
  NO CARRIER / QMI "Call failed" were RADIO, not software. Smoking gun:
  +CGATT: 0 while +CEREG: 0,1, and a fresh-cid AT+CGACT=1,5 returning
  "+CEER: NAS Service request failed, no throttle" -- at RSRP ~-120dBm
  the radio cannot complete the NAS service request every dial needs;
  the firmware fails fast with no cause. The "active" cid1 PDN with a
  real 2607:fb90:: address is preserved ESM state from a lucky attach
  (IMS rode the same luck -> calls worked "eventually"). Also verified
  en route: WDA data format is raw-ip on LE20B04SIM7600G22 firmware,
  QMI control path works fine under QEMU passthrough, ModemManager
  leftovers were NOT the cause (inactive + fresh power cycles).
- S45modem: ensure_attached() gate -- CEREG can read registered while
  CGATT: 0 (observed zombie state where every dial is doomed); each
  round now checks and forces AT+CGATT=1, and diagnostics log cgatt

---

immutable rootfs + on-device system updates (branch immutable_n_updates)

Read-only squashfs root verified by dm-verity, /NeoDCT/User on its own
partition, and .ndsw update packages installed from an SD card. QEMU-only
so far; the Luckfox NAND/UBI layout is deliberately untouched.

Why an initramfs. You cannot dd over the squashfs the kernel is
demand-paging from, so SystemUpdate never writes the system partition. It
verifies a package, copies the image to the user partition as a pending
update and reboots; a ~1.3MB initramfs applies it before the root is
mounted. That makes the write power-fail safe: the staged image and its
record are only deleted after the write has been read back off the device
and hashed, so an interrupted install just happens again next boot, and a
write that keeps failing is abandoned after three attempts.

Single slot, not A/B. Two ~50MB slots do not fit in the Pico Mini's 128MB
NAND alongside userdata, and a design that only works on QEMU would have
to be thrown away for hardware.

Update format. UPDATE.ndsw is a zip of rootfs.squashfs (padded squashfs
with the dm-verity hash tree appended), manifest.json and manifest.sig.
The manifest carries the image sha256 and the verity parameters, so
signing the manifest covers the image. Two fields beyond the proposed
format: "platform" (qemu-aarch64 / luckfox-armv7), refused on a mismatch
because both images come out of this repo with the same filename and one
wrong copy onto real hardware is a reflash; and optional "min_kernel".

Signing is RSA-4096/SHA-256 verified by a dependency-free PKCS#1 v1.5
verifier (System/core/UpdateService/signing.py) -- real gnupg2 on the
device would have cost 4-6MB of libgcrypt/libassuan/libksba for the same
security. It rebuilds the whole expected encoded block and compares it
whole, so Bleichenbacher'06-style short-padding forgeries and SHA-1
algorithm substitution are both rejected (tested). Releases are signed
with `openssl dgst -sha256 -sign`; NEODCT_SIGN_KEY unset still builds a
package, just unsigned, which is the BAD SIGNATURE path on purpose.

dm-verity is enforced by default (neodct.verity=enforce; permissive and
off exist as escape hatches, and a failure under enforce drops to a
rescue shell rather than a reboot loop). Honest scope: the root hash
comes from installed.prop on the user partition, so this is integrity,
not authenticity -- it catches NAND bit-rot, partial writes and corrupt
flash on every block read, while authenticity comes from the signature
check the app does before staging. Anyone able to rewrite the user
partition can rewrite the system partition too. The upgrade is
CONFIG_DM_VERITY_VERIFY_ROOTHASH_SIG with the release cert built into the
kernel, which needs no userspace crypto at all.

Version metadata had to move. system.os.* lived in
/NeoDCT/User/settings.prop, which only appeared to work because an update
replaced the whole filesystem including that file. With /NeoDCT/User on
its own partition it survives an update, so the phone would have reported
its factory version forever. Those keys now come from a read-only
/NeoDCT/System/version.prop generated at build time from os-release
VERSION_ID; SettingsStorage layers defaults < settings.prop <
version.prop and drops stale system.os.* keys it finds.

Read-only fallout fixed: no more `mount -o remount,rw /` in either
inittab; crash log to /NeoDCT/User/logs; PYTHONPYCACHEPREFIX to
/NeoDCT/User/.pycache (without it a read-only root re-parses the whole UI
every boot); S45modem's cache off /etc; host-built __pycache__ pruned
from the image (host python 3.14 != target 3.13.11 anyway). Stock
wallpapers moved to /NeoDCT/System/wallpapers -- they are image content,
like System/tones. The shipped seed DBs and settings.prop were dropped:
init_databases() already creates schemas and DEFAULTS covers the rest, so
an empty user partition self-provisions.

SD card: /NeoDCT/System/hw/neodct-sdcard mounts anything that is not the
system or user partition at /NeoDCT/User/sdcard and publishes state to
/run/neodct/sdcard.prop for System/core/Storage. Boot scan (S06sdcard) +
udev rule for hotplug. Its parent-disk logic is tested: mmcblk0p1 is
recognised as part of mmcblk0, so "format this card" can never point
mkfs.vfat at a partition the phone is running from. Settings > Memory
card offers folder setup for a plain card (non-destructive) and
reformatting only for one that cannot be read.

Apps: SystemUpdate (id 12, placeholder icon -- wants real art). Music now
requires a card and reads sdcard/music. Tones gains an "Add more..." row
and Settings/Wallpaper a "Get more...", both explaining the card in a
TextScroller. Dialog wording is per spec and pinned by tests: INVALID
UPDATE and WRONG UPDATE are dead ends with OK and no cancel key, BAD
SIGNATURE is overridable with an Acknowledge softkey in engineering mode
only.

Build: BR2_TARGET_ROOTFS_SQUASHFS (zstd), lvm2 without STANDARD_INSTALL
(dmsetup only, ~400KB instead of all of cryptsetup's dependency tree),
dosfstools, host-mtools/dosfstools/e2fsprogs. Kernel gains SQUASHFS
(zstd+xz+zlib), DM_VERITY+CRYPTO_SHA256, BLK_DEV_INITRD, VFAT+NLS.
post-image-neodct.sh assembles system.img, initramfs.cpio.gz,
userdata.ext4 (label NDUSER, carrying the factory installed.prop) and a
ready-made sdcard.img, which it never overwrites if one already exists.
`make update` packages the built tree as UPDATE.ndsw. Boot it with
neodct/tools/run_qemu.sh (NEODCT_SD=share also mounts a host folder as
the card over virtiofs -- CONFIG_VIRTIO_FS was already enabled).

Tests: 113 -> 314. The parts that can brick a phone are tested rather
than trusted -- the verity hash tree is byte-for-byte identical to
veritysetup 2.8.6 (golden vectors plus a live cross-check), the shell
applier's dm-verity table is compared against the Python implementation
of the same layout, and the applier itself is driven on the host with
files standing in for block devices (corrupt image refused without
touching the device, failed write retried, injection canary in the
records). One test runs the whole feature: the app stages a package and
the real busybox applier installs it.

Not done: no full buildroot build has been run yet, so nothing here has
booted -- the image set was verified by assembling it against a fake
target tree. Luckfox phase 2 (UBI/ubiblock layout, SQUASHFS+DM_VERITY+
VFAT in the 5.10 SDK kernel, initramfs into boot.img, SDMMC pins) is
untouched. No initramfs SD-card recovery: an unbootable system partition
means a USB reflash.

run_qemu.sh reconciled with the known-good ext4 invocation: virtio-gpu-pci
/ virtio-keyboard-pci / virtio-tablet-pci (PCI, not the MMIO variants),
-m 72, video=Virtual-1:240x175M (not 240x240 -- on QEMU the framebuffer is
the UI band directly, there is no panel to letterbox into), both consoles
plus vt.global_cursor_default=0 and quiet/loglevel=0, xhci + usb-audio via
pulse, and optional SIM7600 passthrough (NEODCT_MODEM=1). Networking is off
by default to match that invocation; NEODCT_NET=1 adds the NIC back for
browser testing. root= is gone -- the initramfs mounts everything.

Added neodct/tools/mkbadupdate.py (11 deliberately broken .ndsw variants:
unsigned, wrong-key, tampered-manifest, corrupt-image, truncated-image,
no-manifest, no-image, not-a-zip, wrong-platform, future-kernel,
bad-root-hash), neodct/tools/sdcard.sh (put/ls/rm/init/new on sdcard.img via
buildroot's host mtools -- no sudo, no loop mount) and
docs/TESTING_UPDATES.md walking through building, installing and breaking an
update by hand. test_mkbadupdate.py asserts every variant still triggers its
specific refusal, so the manual matrix in that doc cannot silently rot.
Variants that edit the manifest are re-signed so they fail for exactly one
reason. bad-root-hash is the one that proves verity is on: it passes every
check the app makes and only fails at boot.

First real boot found two initramfs bugs, both now covered by tests:

* No library search aliases. glibc's built-in path here is /lib64 and
  /usr/lib64, and an initramfs has no /etc/ld.so.cache to redirect it, so
  libraries in /lib were invisible and boot died at "/bin/sh: error while
  loading shared libraries: libresolv.so.2". The target tree solves this with
  lib64 -> lib symlinks; mkinitramfs now mirrors them (lib64, usr/lib,
  usr/lib64 all -> lib) so it does not matter which path the loader was built
  with, or which libc.
* Three applets used by the scripts (wc, tr, dirname) had no busybox symlink.
  That class of bug can be silent rather than loud: `wc` failing inside a
  command substitution yields an empty size, which reads as a truncated image
  and discards a perfectly good update. test_initramfs_applets.py now scans
  both scripts for command names and fails if APPLETS does not cover them, so
  the list cannot drift from the code again. Dropped findfs, which this
  busybox does not build and nothing calls.

Second real boot: the system image was never found. "no /sbin/init in the
system image" was the initramfs having mounted the SD card as root --
virtio-mmio devices are NOT enumerated in the order QEMU is handed them, so
neodct.sys=/dev/vda pointed at the wrong disk. (Confirmed by verity=off
failing identically, and by system.img starting with "hsqs" as it should.)

Device names from the cmdline are now a hint only. find_system_device()
identifies the system image by its squashfs magic ("hsqs", conveniently
printable, so no od needed) and find_user_device() by the NDUSER label, both
falling back to scanning every block device. Chosen devices are always
logged, since that is the first thing worth knowing when this goes wrong.
This is also more robust for real hardware, where the names differ again
(mmcblk, ubiblock).

Added recovery mode (neodct/initramfs/ndsys-recovery.sh), following the menu
in the rec.py prototype -- update system / wipe user data / wipe system /
reboot / shell, arrow keys or j/k, confirmations defaulting to "no". Shell
rather than python (there is no python in an initramfs) and text rather than
framebuffer graphics, because CONFIG_FRAMEBUFFER_CONSOLE is on, so writing to
/dev/tty1 already appears on the phone's screen. Every string is written to
fit 30x10, which is what a 240x175 panel gives in the 8x16 font.

init now enters recovery instead of a bare rescue shell whenever an SD card
could fix the problem: no system image found, verity refused it, the root
would not mount, or no /sbin/init. neodct.recovery=1 forces it.

recovery_install_package() is separated from the menus and unit tested (10
tests): it hashes the image straight out of the zip BEFORE writing anything,
writes, reads back and re-hashes, then records installed.prop and clears any
stale pending update. It cannot check signatures -- no crypto in the
initramfs -- so it is an integrity check gated on physical access, and the
confirmation screen says so.

Also: "wipe user data" preserves .ndsys, or the next boot would have no
installed.prop, fail verity and land straight back in recovery.

Third boot: it works. Content-based detection found the system on /dev/vdc
(the device order was fully reversed -- vda was the 128MiB card, vdb the
512MiB userdata, vdc the 134MiB system image), dm-verity loaded, and the
launcher came up under verity=enforce.

The card was still missing, though, for the same root cause one layer up:
neodct-sdcard excluded devices by the cmdline names, so it skipped the real
card and tried to mount the system image. It now excludes by content as well
-- squashfs magic, the mount table, and whatever the initramfs recorded --
and init publishes sys_device/user_device into verity_state.prop so userspace
and the initramfs cannot disagree about which disk is which.

Two more read-only artifacts the boot log exposed: /dev/shm had no mountpoint
(devtmpfs hides the one in the image, so inittab makes it at runtime) and
seedrng could not persist its seed (/var/lib/seedrng is now a symlink to
/NeoDCT/User/.seedrng, created by S05userdata).

post-image no longer silently resets user data on every build: it warns that
it did, and NEODCT_KEEP_USERDATA=1 keeps the partition, refreshing
.ndsys/installed.prop in place with debugfs. That refresh is not optional --
installed.prop records the root hash of the image that was installed, so a
kept partition plus a rebuilt image would fail verity on the next boot.

App folder renamed SystemUpdate -> Update (display name unchanged).

Fourth boot: the card was found (/dev/vda) but announced as having "no
filesystem we can read". The image was a perfectly valid FAT32 -- util-linux
blkid reports TYPE="vfat" and mtools lists the folders -- but *busybox's*
blkid has far fewer filesystem probers and reported nothing at all, so the
empty-fstype branch fired.

blkid was the wrong question. try_mount now asks the kernel: it tries vfat,
exfat, then the ext family, then lets the kernel pick from its own probe
list, and only reports unmountable when all of that fails. FAT-only options
(utf8, flush) are gated per filesystem, since passing them to ext4 fails the
mount. blkid is now only consulted for the label, which is cosmetic.

Tested by stubbing `mount` as a shell function (functions win over PATH), so
the ordering, the per-filesystem options and the recorded state are all
checked on the host without needing root.

Also: S05userdata -> S00userdata. buildroot's seedrng script is S01, so it
ran before the directory its symlink target needs existed; .seedrng is now in
the factory skeleton too, so a fresh partition has it before anything runs.

"staged update is incomplete; discarding" was a real staged update being
thrown away, not a signature check. The record stores image=, and the app
writes it as /NeoDCT/User/.ndsys/pending.img -- correct where the app runs,
meaningless in the initramfs, which has the same partition mounted at
/mnt/user. The `[ ! -f "$image" ]` guard therefore always failed.

Both sides now resolve the image against whichever state directory they are
reading, never trusting the recorded string: Record grew a state_dir and an
image property, and the shell applier joins basename onto its own STATE_DIR.
The record still carries the path for readability. Two tests reproduce the
exact failure by rewriting a staged record to the device-side path.

Worth noting for the future: this is the third bug of the same shape -- a
name that is only valid in one context (device order, loader search path,
mount namespace). Anything crossing the initramfs/system boundary should be
treated as needing re-resolution, not re-use.

Recovery entry points wired up: NEODCT_RECOVERY=1 (neodct.recovery=1 on the
cmdline) and a one-shot flag file, /NeoDCT/User/.ndsys/boot_recovery, which is
consumed as it is read so rebooting out of recovery does not loop back in.
The flag is the route that works on real hardware, where the cmdline cannot be
edited. neodct.rectty=/dev/console (NEODCT_RECTTY) drives the menu over serial
if keys are not reaching the framebuffer VT. docs/TESTING_UPDATES.md gained a
recovery section.

Recovery navigation: arrow keys echoed as ^[[A and the menu appeared frozen.
Raw mode was only applied to the primary tty, but in QEMU both the framebuffer
VT (tty1) and the serial console are live and there is no way to know which
one the user is typing into -- and without raw mode `dd bs=1 count=1` blocks
for a whole line, which an arrow key never completes.

Now every candidate tty gets raw -echo and the menu is drawn to all of them.
Digits also move the selection (Enter still selects), which works even if raw
mode fails entirely: "3<Enter>" reads identically in raw and line-buffered
modes, and unlike select-on-digit a stray Enter cannot pick something by
accident. j/k still work for terminals with no arrow keys.

Nine tests drive recovery_menu with scripted keys -- wrapping, digits,
out-of-range digits, digit-then-arrow. Writing them exposed read_key stashing
its value in a global for the digit case to read back; the menu now captures
the key once and cases on it.

Recovery input rewritten rather than patched again. The old version read one
tty and put only that one in raw mode, but in QEMU the framebuffer VT (tty1)
and the serial console are both live and there is no way to know which the
user is typing into -- so half the time arrows echoed as ^[[A and Enter did
nothing.

Now every candidate tty is put in raw mode and pumped by its own `cat` into a
single FIFO, which the menu reads as fd 8. The FIFO is opened O_RDWR so the
open cannot block and read_key never sees EOF. Typing works in either window.
Without mkfifo it degrades to reading the primary tty directly.

Three bugs surfaced while writing the tests for it:

* LF=$(printf '\n') is the empty string -- command substitution strips
  trailing newlines -- so the Enter-as-LF case could never match and Enter
  presses arriving as \n were silently swallowed. Now a literal newline.
* ESC O A (application cursor mode, which some emulators send) was not
  handled at all, only ESC [ A.
* mkfifo had no busybox symlink, so the merge would have silently fallen back
  to one tty. The applet scanner caught it, which is what it is for; kill was
  added alongside it.

Also: recovery_confirm still wrote its warning straight to $TTY, so on a
two-tty setup the question appeared on one screen and the yes/no on both, and
it carried a dead `for line in $1; do :; done`. It uses say() now. The rescue
shell stops the pumps before handing over and restarts them after, or they
would fight over every keystroke.

read_key is now driven from a byte stream in tests (a regular file on fd 8),
which also pins that the fd is opened once and read sequentially -- reopening
per call would restart at byte 0 and parse the same key forever.

---

initramfs: identify the system device by disk serial, and read recovery's
keys off the screen it draws on

Two faults, both of which made recovery unusable for the thing it exists to
do -- reinstalling a phone that cannot boot.

## The system device could not be found once it was wiped

find_system_device identified the system image by its squashfs magic, which
works right up until "wipe system" zeroes it -- exactly when knowing the
answer matters. The cmdline hint is no fallback: QEMU does not enumerate
virtio-mmio devices in the order they are given, so `system, user, card`
comes up in the guest as vda=card, vdb=user, vdc=system, and neodct.sys=
/dev/vda has always pointed at the wrong disk. Confirmed by reading
/sys/block/*/serial in a booted guest. With a wiped image and no serials the
initramfs picked /dev/vda and would have installed onto the SD card.

The drives now carry a disk serial (NDSYS / NDUSER / NDCARD, set with
-device virtio-blk-device,serial=), read back from /sys/block/*/serial. A
serial belongs to the device rather than to anything written on it, so
zeroing the image cannot take it away, and it outranks both the hint and the
content scan -- which also stops an SD card someone has written a system
image onto from being adopted as the root filesystem.

The scan and the hint stay as fallbacks: an eMMC partition has no disk
serial, and on real hardware the names are stable anyway, which is why this
was only ever a QEMU problem.

Verified in QEMU with the first megabyte of system.img zeroed: `system=
/dev/vdc` with serials, `system=/dev/vda` (the card) without.

## Arrow keys did nothing on the phone's screen

Two independent reasons, either of which was enough on its own.

The terminal would not stay in character-at-a-time mode. `stty -F /dev/tty1
raw -echo`, and equally `stty ... < /dev/tty1`, opens the terminal, sets the
mode and closes it -- and the VT console driver sets TTY_DRIVER_RESET_TERMIOS
(drivers/tty/vt/vt.c), so closing the last descriptor puts it straight back
to canonical with echo on. The mode was gone before anything read a byte,
which is why every arrow press printed ^[[B over the menu and nothing was
readable until Enter. recovery_input_start now holds the terminal open on fd
8 for as long as recovery runs and sets the mode through that descriptor.

And the keys were read from the shell's stdin, which for init is /dev/console
-- the serial port. The screen's VT was never read at all, so input "worked"
over serial and did nothing on the phone. read_key reads fd 8.

read_key is back to dd rather than `read -n 1`: this busybox is built without
CONFIG_ASH_READ_NCHARS, so ash's read has no -n and takes a whole line.

The pump-and-FIFO merge is gone with it. Reading both ttys needed two readers
merged into one stream, and it wedged in the guest -- the console pump sat in
pipe_lock in D state, blocking the shared FIFO for everything else. Recovery
now drives one terminal, drawn on and read from; neodct.rectty=/dev/console
moves the whole UI to the serial port, which is what a headless run wants.
That also fixes the menu being painted twice, once on the display and once in
the terminal QEMU was started from.

## Tests

Device identification is driven against a stand-in /sys/block and /dev,
including the wiped case and a squashfs planted on the card.

The input path is driven through a real pty, with a shim that gives it the
one property of a VT that matters here: a mode set through a descriptor
nobody holds is undone when the setting process exits. Without it the bug is
invisible on the host, because a pty keeps whatever it is told. The pty test
reproduced the exact symptom -- b'^[[B' echoed back, no key delivered.

Verified end to end in QEMU: arrows move the selection on the display, Enter
on "reboot" reboots, and nothing is painted on the serial console.

================================================================================

update: a software-update page worth reading, and pictures in .ndsw

The update app worked and looked like a debugging tool. Installing meant
paging through five screens of release notes at 20px, then answering four
questions -- install?, back up?, the backup's own "done" dialog, restart? --
each one a modal with a warning triangle on it, whether or not anything was
wrong. The progress bar, the one thing that already looked right, had "0.0 MB
of 0.0 MB" drawn straight through it.

It is now one page and one bar.

## The page

DetailPage (framework) is a page you read: a picture, a title, a couple of
lines of detail beside it, then body text, all in one column that scrolls a
line at a time with up/down and carries the Nokia scrollbar when there is
more below. The update page is a picture, the version, size and build date,
"Verified" or "Not signed", and the release notes -- softkey Install.

Three rules in it are worth knowing, because each one is a thing that looked
wrong before:

* The hero row shrinks until the body starts on the same screen. Picture and
  details stacked, they filled the screen on their own and everything worth
  reading was below the fold; beside each other they cost 72 pixels. If the
  details still do not fit, the picture gives ground rather than the text.
* A block that would be sliced by the bottom edge is left for the next
  scroll. Half a line of type at the fold reads as a rendering bug.
* A page that fits is centred. Three words pinned to the top of an otherwise
  black screen reads as a crash.

The same paragraph rule went into TextScroller, which every other app's help
text still uses: a blank line is a gap of a third of a line, not an empty
line of 20px type. Four short paragraphs used to need three screens.

## The bar

ProgressScreen (framework) keeps the bar and gives it room: step label above,
percentage and byte count below, hint at the foot, nothing on top of it. One
screen covers the whole install -- "Backing up data" then "Copying update"
via set_step, not two screens that flicker between them. It repaints only
when the whole percentage changes, so the copy loop can call it per 256KB
chunk without slowing the copy down. Labels and hints drop a font size, then
ellipsize, rather than running off both edges.

## The backup is not a question

It happens while the bar is on screen. User data is on its own partition and
an update does not touch it, so the backup is belt and braces -- which means
a full or write-protected card is no reason to refuse to install. It is best
effort, and a failure is admitted on the last page instead of interrupting.

## Signature warnings

Same warning either way, "OK" on both -- "Acknowledge" was a word from a
lawyer. Outside engineering mode that dialog is the end of it. In
engineering mode it is followed by "Install Anyway?", which C cancels. An
unsigned image is how you end up stuck on a phone that will not boot, and a
dead phone cannot be talked out of it afterwards.

INVALID UPDATE and WRONG UPDATE FOR THIS PHONE are unchanged and still have
no way past them.

## Pictures in packages

.ndsw can carry thumbnail.png, a square PNG the phone draws at 64x64. Its
sha256 goes in manifest.json, so the one signature over the manifest covers
the art as well; write_ndsw is the only place that knows about both, so the
bytes written and the bytes signed cannot disagree. Art with no hash recorded
is treated as absent, and art that does not match its hash is dropped in
favour of the app icon -- it is decoration, and the image's own sha256 is
what says whether an update is safe. There is a 256KB cap read from the zip
directory, so an oversized "thumbnail" is refused without being decompressed.

`make update` picks up neodct/release-thumbnail.png if it exists, or
NEODCT_UPDATE_THUMBNAIL=..., and mkupdate refuses anything that is not a PNG
or is over the cap rather than shipping art the phone will not draw.

## Tests

Three suites, all TDD'd, 82 tests:

* test_update_ui.py -- the widgets, judged on real drawn frames from the real
  font: no text within three pixels of the bar, nothing in the softkey bar,
  the notes start on the first screen, the bottom line is never cut in half.
* test_update_flow.py -- whole flows through the shipped app against a staged
  card with a real signed .ndsw on it. Installing is two key presses; the
  whole install is two screens and a bar (repaints are not screens); the
  release picture reaches the screen; the databases are on the card
  afterwards.
* test_systemupdate_app.py -- the policy, rewritten for the new flow: what is
  refused, what engineering mode may walk past, and that nothing asks about
  the backup.

tests/update_ui_fixtures.py stages the card, the release key and the version
file and hands back the real UI; NEODCT_UI_SHOTS=/some/dir writes every
screen the tests draw to PNG, which is how the design was reviewed.

Two harness fixes fell out of it: uistub's PathRemap now covers io.open (what
zipfile actually calls -- a package on the card was unreadable through the
stub) and shutil.copy2 (the backup), and test_sdcard_helper no longer leaves
its tmp path in Storage.MOUNT_POINT for the rest of the suite to trip over.

482 tests pass.

================================================================================

0.3.1a: the first release installed by the update system it ships

Version bumped in the one place a version is supposed to be bumped --
VERSION_ID in overlay/etc/os-release -- plus the SettingsStorage fallbacks and
a CHANGELOG section, which is what mkupdate reads to fill the release notes on
the update page.

Two things that had drifted are now derived instead of typed:

* The boot splash had "System v0.3.0a" written into launcher.py, so it was a
  release behind from the moment anything else moved. It reads
  system.os.versionnumber now, like every other screen that shows a version.

* **system.os.platform was a path on my machine.** Buildroot calls post-build
  scripts as `script TARGET_DIR $BR2_ROOTFS_POST_SCRIPT_ARGS
  $BR2_ROOTFS_POST_BUILD_SCRIPT_ARGS`, and the qemu board needs the defconfig
  path in POST_SCRIPT_ARGS -- so $2 was that path and the platform was $3.
  post-build-system-metadata.sh read $2, so every image built here recorded
  system.os.platform=/home/bubbles/.../neodct_qemu_defconfig.

  That is the value an update's manifest is compared against, and a platform
  mismatch is the one refusal with no way past it, on purpose: it is the brick
  case. So an update built in a different directory -- or by anyone else --
  would have been refused as the wrong hardware, and the phone would have said
  WRONG UPDATE FOR THIS PHONE with no explanation that made any sense. The
  script now takes the last argument and refuses anything with a slash in it,
  because a platform id has to mean the same thing on every machine.

  The catch is that the fix cannot travel by update: the check compares the
  new manifest against the running image, so the first image with a corrected
  platform is refused by any phone still carrying the old value. Nothing has
  shipped to anyone, so the 0.3.0a base was simply rebuilt with the fix before
  0.3.1a was built on top of it.

## Tests

test_post_build_metadata.py drives the script the way buildroot drives it, and
pins the argument order that caused this -- including that a path is never
recorded as a platform, whatever position it arrives in.

test_settings_version_layering.py gained the splash: it reads the version out
of the image, and still says something when there is no version.prop.

490 pass.

## The release

  buildroot/output/images/UPDATE.ndsw   0.3.1a, qemu-aarch64, 57.5 MiB, signed
                                        with the dev key, carrying a 64x64
                                        thumbnail.png the manifest vouches for

output/images/system.img and userdata.ext4 are the rebuilt **0.3.0a** pair, so
the phone boots the old version and the card has the new one on it. The
pristine base is kept in output/images-0.3.0a/ to run the install again.
